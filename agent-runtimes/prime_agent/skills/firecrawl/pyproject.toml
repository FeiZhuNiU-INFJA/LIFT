[project]
name = "prime-agent-skill-firecrawl"
version = "0.1.0"
description = "Firecrawl integration skill for Prime Agent (Firecrawl's official hosted MCP server)."
requires-python = ">=3.10"
dependencies = ["mcp", "httpx", "prime-agent-runtime"]

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"

[tool.hatch.build.targets.wheel]
packages = ["src/firecrawl"]
